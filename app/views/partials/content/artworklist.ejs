<article class="sidebar">
  <h1 class="sidebar-title">Safety First</h1>
  <p class="sidebar-body">All tourists safety was our first priority, all necessary safety precations were done including medical service, emergency evacuation was also provided.</p>
  <div class="pixgrid clearfix">
    <% if (artwork.length > 0) { %>
    <% for (i=0; i< artwork.length; i++) { %>
        <img src="/images/artwork/<%= artwork[i] %>" alt="Artwork <%= i %>">
    <% } %>
    <% } %>
  </div>
</article>
